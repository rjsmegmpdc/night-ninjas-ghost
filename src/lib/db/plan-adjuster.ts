/**
 * plan-adjuster.ts — SIGNAL's file (stub)
 *
 * This stub satisfies imports on the feat/coaching-ui branch. The real
 * implementation is built by SIGNAL in feat/signal-coach-triggers and will
 * replace this file on merge.
 */

import type { CoachAdjustment } from '@/lib/ai/coach-triggers';

/**
 * Applies a coach-suggested adjustment to the active training plan.
 * STUB — no-ops until SIGNAL's implementation lands.
 */
export async function applyCoachAdjustment(_adjustment: CoachAdjustment): Promise<void> {
  // no-op stub
}
